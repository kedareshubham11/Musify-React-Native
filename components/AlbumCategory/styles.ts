import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({

    container: {
        margin: 0,
    },

    title: {
        color: "#ffff",
        fontSize: 26,
        fontWeight: 'bold',
        margin: 10
    }

});

export default styles;